diff --git a/Source/HBIOS/tms.asm b/Source/HBIOS/tms.asm
index 3d16b75..e4799dd 100644
--- a/Source/HBIOS/tms.asm
+++ b/Source/HBIOS/tms.asm
@@ -318,18 +318,34 @@ TMS_VDASCR2:
 ; DUMMY FUNCTIONS BELOW BECAUSE SCG BOARD HAS NO
 ; KEYBOARD INTERFACE
 
+; TMS_STAT:
+; 	XOR	A		; SIGNAL NOTHING READY
+; 	JP	CIO_IDLE	; DO IDLE PROCESSING
+
+; TMS_FLUSH:
+; 	XOR	A		; SIGNAL SUCCESS
+; 	RET
+
+; TMS_READ:
+; 	LD	E,26		; RETURN <SUB> (CTRL-Z)
+; 	XOR	A		; SIGNAL SUCCESS
+; 	RET
+
+
 TMS_STAT:
-	XOR	A		; SIGNAL NOTHING READY
-	JP	CIO_IDLE	; DO IDLE PROCESSING
+	LD	C,1			; CONSOLE UNIT TO C
+	LD	B,BF_CIOIST		; HBIOS FUNC: OUTPUT CHAR
+	JP	CIO_DISPATCH		; CALL CIO DISPATCHER DIRECTLY
 
 TMS_FLUSH:
 	XOR	A		; SIGNAL SUCCESS
 	RET
 
 TMS_READ:
-	LD	E,26		; RETURN <SUB> (CTRL-Z)
-	XOR	A		; SIGNAL SUCCESS
-	RET
+	LD	C,1			; CONSOLE UNIT TO C
+	LD	B,BF_CIOIN		; HBIOS FUNC: OUTPUT CHAR
+	JP	CIO_DISPATCH
+
 ;
 ;======================================================================
 ; TMS DRIVER - PRIVATE DRIVER FUNCTIONS
