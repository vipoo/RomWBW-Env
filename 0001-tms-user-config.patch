diff --git a/Source/HBIOS/tms.asm b/Source/HBIOS/tms.asm
index 9a0ec46..391f775 100644
--- a/Source/HBIOS/tms.asm
+++ b/Source/HBIOS/tms.asm
@@ -22,6 +22,16 @@ TMSINTEN:       .EQU   5               ; INTERRUPT ENABLE BIT
 	.ECHO	"TMS INTERRUPTS ENABLED"
 #ENDIF
 
+#IF (TMSMODE == TMSMODE_USR)
+TMS_DATREG	.EQU	TMS_USRDATREG
+TMS_CMDREG	.EQU	TMS_USRCMDREG		; READ STATUS / WRITE REG SEL
+
+TMS_PPIA	.EQU	TMS_USRPPIA		; PPI PORT A
+TMS_PPIB	.EQU	TMS_USRPPIB		; PPI PORT B
+TMS_PPIC	.EQU	TMS_USRPPIC		; PPI PORT C
+TMS_PPIX	.EQU	TMS_USRPPIX		; PPI CONTROL PORT
+#ENDIF
+
 #IF (TMSMODE == TMSMODE_RC)
 ;TMS_DATREG	.EQU	$BE		; READ/WRITE DATA
 ;TMS_CMDREG	.EQU	$BF		; READ STATUS / WRITE REG SEL
